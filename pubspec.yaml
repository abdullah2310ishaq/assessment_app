name: numerology_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+15

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  video_player: ^2.7.2
  google_fonts: ^4.0.4
  shared_preferences: ^2.2.2
  flutter_widget_from_html: ^0.14.11
  open_file: ^3.3.2
  font_awesome_flutter: ^10.4.0
  intl: ^0.18.1
  permission_handler: ^11.0.1
  in_app_update: ^4.2.3
  file_saver: ^0.2.6
  # Firebase dependencies for AOM Founder Family app
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  firebase_auth: ^4.15.3
  firebase_messaging: ^14.7.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1   # 👈 yeh line add ki

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/chapters/
    - assets/logo.jpg   # 👈 logo ko asset me add kiya

flutter_launcher_icons:   # 👈 icon configuration
  android: true
  ios: true
  image_path: "assets/logo.jpg"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/logo.jpg"
